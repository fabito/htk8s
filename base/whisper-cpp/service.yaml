apiVersion: v1
kind: Service
metadata:
  labels:
    run: whisper-cpp
  name: whisper-cpp
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: whisper-cpp
  type: ClusterIP
